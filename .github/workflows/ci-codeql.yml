name: CI CodeQL

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: '44 9 * * 0'
  
permissions:
  contents: read

jobs:
  analyse:
    name: Analyze (${{ matrix.language }})
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - language: actions
            build-mode: none
    uses: touchtheduck/.github/.github/workflows/reusable-codeql.yml@a4fa5d8fedf647bac79f9dcd1c51290d4a9b39a9 # main
    with:
      language: ${{ matrix.language }}
      build-mode: ${{ matrix.build-mode }}